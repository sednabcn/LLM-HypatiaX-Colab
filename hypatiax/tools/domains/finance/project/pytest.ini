[pytest]
minversion = 6.0
addopts = -q --color=yes --maxfail=1 --strict-markers
# When running CI/coverage use: pytest --cov=uniswap_v2_formulas_extended --cov-report=term-missing
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    benchmark: marks benchmarks
