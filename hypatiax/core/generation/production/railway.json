# DAY 2: DEPLOYMENT (8 hours)
# Option 1: Railway (Fastest - 30 minutes)
# 1. Create railway.json
{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn api.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health"
  }
}

# 2. Deploy
npm i -g @railway/cli
railway login
railway init
railway up

# Live in 2 minutes at: https://your-app.railway.app
